database model with mongodb for the connect app

_______

USERS
{
    _id: ObjectId,
    email: string,
    password_hash: string,
    first_name: string,
    last_name: string,
    birth_date: date,
    profile_picture: string,
    bio: string,
    roles: array[string],
    tutor_profile: {
        description: string,
        subjects: array[{subject_id: ObjectId, subject_name: string}],
        certifications: array[string],
        average_rating: float,
        reviews_count: int,
        cv_path: string,
    } | null,
    tutor_application: array[{
        subject_id: ObjectId,
        subject_name: string
        status: string,
        assigned_date: date,
        started_date: date | null
        expire_date : date | null
        completed_date: date | null,
        test_data: array[{
            question_id: ObjectId,
            question: string,
            answers: array[{
                answer_id: int,
                text: string
            }]
        }],
        user_answers: array[{
            question_id: ObjectId,
            answer_given: int
        }]
    }] | null,
    payment_methods: array[{
        token_id: string,
        type: string,
        expiration_date: string // Mese/Anno    (es: "12/28"),
        provider: string,
        last_four_digit: string,
        is_default: boolean
    }],
    coins: int,
    creation_date: date,
    last_access: date
}

SUBJECTS
{
    _id: ObjectId,
    name: string,
    field: string
}

CLASSROOMS
{
    _id: ObjectId,
    subject_id: ObjectId,
    name: string,
    members: array[
    { user_id: ObjectId, role: string, join_date: date }
    ],
    description: string,
    invite_code: string,
    creation_date: date
}

CLASSROOMS_DOCUMENTS
{
    _id: ObjectId,
    classroom_id: ObjectId,
    owner_id: ObjectId,
    filename: string,
    storage_path: string,
    upload_date: date
}

CONVERSATIONS
{
    _id: ObjectId,
    type: string,
    classroom_id: ObjectId | null,
    participants: array[ObjectId],
    creation_date: date,
    last_activity: date
}

MESSAGES
{
    _id: ObjectId,COINS_TRANSACTIONS
{
    _id: ObjectId,
    type: string,
    from_user: ObjectId | null,
    to_user: ObjectId | null,
    coins: int,
    creation_date: date
}

TUTORING_SESSIONS
{
    _id: ObjectId,
    tutor_id: ObjectId,
    student_id: ObjectId,
    start_time: date,
    end_time: date,
    duration_minutes: int,
    status: string
}
    conversation_id: ObjectId,
    sender_id: ObjectId,
    text: string,
    attachments: array[string],
    creation_date: date
}

TUTOR_TESTS
{
    _id: ObjectId,
    subject_id: ObjectId,
    questions: array[
        {
            _id: ObjectId,
            question: string,
            answers : array[{
                answer_id: int,
                text: string
            }],
            correct_answer: int
        }
    ]
}

TESTS
{
    _id: ObjectId,
    title: string,
    classroom_id: ObjectId,
    creator_id: ObjectId,
    creator_name: string,
    questions: array[
    {
        _id: ObjectId,
        question_type: string,
        question: string,
        max_score: int,
        answers: array[{
            answer_id: int,
            text: string
        }] | null,
        correct_answer: int | null
    }],COINS_TRANSACTIONS
{
    _id: ObjectId,
    type: string,
    from_user: ObjectId | null,
    to_user: ObjectId | null,
    coins: int,
    creation_date: date
}

TUTORING_SESSIONS
{
    _id: ObjectId,
    tutor_id: ObjectId,
    student_id: ObjectId,
    start_time: date,
    end_time: date,
    duration_minutes: int,
    status: string
}
    time_limit: int,
    is_active: boolean
}

TESTS_SUBMISSIONS
{
    _id: ObjectId,
    title: string,
    test_id: ObjectId,
    student_id: ObjectId,
    answers: array[{
        question_id: ObjectId,
        given_answer_id: int | null,
        given_answer_text: string | null,
        score_assigned: int | null,
        evaluation_status: string
    }],
    submit_date: date,
    status: string,
    score: int
}

STUDENT_TESTS_COMPLETED
{
    _id: ObjectId,
    title: string,
    student_id: ObjectId,
    completed_date: date,
    questions: array[{
        question_text: string,
        given_answer_text: string,
        score_assigned: int
    }],
    status: string,
    score: int
}

TESTS_SESSIONS
{
    _id: ObjectId,
    test_id: ObjectId,
    student_id: ObjectId,
    question_order: array[
        question_id: ObjectId,
        question_type: string,
        question: string,
        max_score: int,
        answers: array[{
            answer_id: int,
            text: string
        }] | null,
    ],
     time_limit: int,
     started_at: date,
     expire_date: date | null
     status: string
}

REVIEWS
{
    _id: ObjectId,
    student_id: ObjectId,
    tutor_id: ObjectId,
    subject_id: ObjectId,
    rating: float,
    comment: string,
    creation_date: date
}

COINS_TRANSACTIONS
{
    _id: ObjectId,
    type: string,
    from_user: ObjectId | null,
    to_user: ObjectId | null,
    coins: int,
    creation_date: date
}

TUTORING_SESSIONS
{
    _id: ObjectId,
    tutor_id: ObjectId,
    student_id: ObjectId,
    start_time: date,
    end_time: date,
    duration_minutes: int,
    status: string
}